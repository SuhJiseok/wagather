# WatchMe 백엔드 문서

## 서버 구성

백엔드는 `server/index.mjs` 파일에 있습니다.

사용하는 주요 구성은 아래와 같습니다.

- Express: 방 생성 API와 웹앱 제공
- Socket.IO: 실시간 입장, 채팅, 재생 상태, 참여자 상태 전달
- Vite middleware: 개발 중 프론트엔드 화면 제공

## 주요 API

### 방 만들기

```http
POST /api/rooms
```

요청 값은 아래와 같습니다.

```json
{
  "youtubeUrl": "https://www.youtube.com/watch?v=...",
  "nickname": "수민"
}
```

응답 값은 아래와 같습니다.

```json
{
  "roomId": "ABC123",
  "inviteUrl": "/room/ABC123",
  "nickname": "수민"
}
```

## 실시간 이벤트

Socket.IO로 주고받는 주요 이벤트는 아래와 같습니다.

- `join-room`: 방 입장
- `room-state`: 현재 방 상태 전달
- `heartbeat`: 각 참여자의 현재 영상 시간과 상태 전달
- `playback-command`: 재생, 일시정지, 구간 이동 전달
- `grant-control`: 방장이 다른 사람에게 조작 권한 부여
- `sync-choice`: 기다리기, 먼저 보기, 다시 합류 상태 전달
- `chat-message`: 채팅 전달
- `emoji-reaction`: 빠른 이모지 반응 전달

## 방 상태 관리

현재 MVP는 방 정보를 서버 메모리에 저장합니다. 즉, 서버를 재시작하면 기존 방은 사라집니다.

저장하는 주요 값은 아래와 같습니다.

- 방 ID
- 유튜브 영상 ID
- 방장 ID
- 현재 조작 권한을 가진 참여자 ID
- 방 기준 재생 상태
- 참여자 목록
- 최근 채팅 메시지

## 동기화 방식

각 참여자는 약 0.9초마다 자신의 현재 영상 시간과 재생 상태를 서버에 보냅니다.

서버는 방 기준 시간과 각 참여자 시간을 함께 내려줍니다. 프론트엔드는 이 차이를 보고 아래 상태를 표시합니다.

- 같이 보는 중
- 앞서 있음
- 뒤처짐
- 잠깐 쉬는 중
- 먼저 보는 중

시간 차이가 3초 이상 나면 사용자는 앞서거나 뒤처진 상태로 표시됩니다.

## 현재 제한 사항

- 서버 재시작 시 방이 사라집니다.
- 데이터베이스가 없습니다.
- 방별 최대 인원 제한은 코드로 강제하지 않았습니다.
- 광고를 직접 감지하거나 제거하지 않습니다.
- 유튜브 외 영상 플랫폼은 지원하지 않습니다.

## 다음 백엔드 개선 후보

- 방 데이터 저장용 데이터베이스 추가
- 방 만료 시간과 정리 정책 강화
- 방별 최대 인원 제한
- 채팅 기록 저장 여부 정책 추가
- 지연 팝업 빈도 제한
- 배포 환경용 설정 분리
